import request from '@/utils/mtop';

export interface IReqData {}

export interface IResResult {}

export const testRequest = (data: IReqData): Promise<IResResult> => {
  return request({
    v: '1.0',
    mock: false,
    needLogin: true,
    api: 'mtop.taobao.a.b.c',
    data,
  });
};

